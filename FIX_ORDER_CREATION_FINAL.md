# Финальное исправление создания заказов

## Что нужно сделать на сервере

### Шаг 1: Обновить код
```bash
cd ~/OkoZnaniy
./update-and-restart.sh
```

Дождитесь завершения (примерно 2-3 минуты). Вы увидите:
```
✅ Проект успешно обновлен и перезапущен!
```

### Шаг 2: Проверить что справочники заполнены
```bash
docker-compose exec backend python manage.py shell
```

В shell выполните:
```python
from apps.catalog.models import Subject, WorkType

print(f"Предметов: {Subject.objects.count()}")
print(f"Типов работ: {WorkType.objects.count()}")

# Должно быть минимум 15 предметов и 10 типов работ
# Если меньше, запустите:
exit()
```

Если справочники пустые, запустите вручную:
```bash
docker-compose exec backend python populate_subjects_and_work_types.py
```

### Шаг 3: Очистить кеш браузера
В браузере клиента:
1. Откройте DevTools (F12)
2. Правый клик на кнопке обновления → "Очистить кеш и жёсткая перезагрузка"
3. Или: Ctrl+Shift+Delete → Очистить кеш

### Шаг 4: Попробовать создать заказ
1. Откройте страницу создания заказа
2. Заполните все поля
3. Нажмите "Создать заказ"

### Шаг 5: Если ошибка осталась - проверить логи

**Логи фронтенда (в консоли браузера):**
```
🔍 showCustomSubject: false
🔍 showCustomWorkType: false
🔍 values.subject_id type: number 1
🔍 values.work_type_id type: number 1
📤 Данные для отправки: {...}
📤 Типы полей: { subject_id: 'number', work_type_id: 'number' }
```

**Логи бэкенда:**
```bash
docker-compose logs backend --tail=100 | grep "OrderSerializer"
```

Должно быть:
```
[OrderSerializer] Validating data: {...}
[OrderSerializer] subject type: <class 'apps.catalog.models.Subject'>, value: Математика
[OrderSerializer] work_type type: <class 'apps.catalog.models.WorkType'>, value: Курсовая работа
```

## Возможные проблемы и решения

### Проблема 1: "объект не существует"
**Причина:** Справочники не заполнены  
**Решение:** Запустить `populate_subjects_and_work_types.py` (см. Шаг 2)

### Проблема 2: Всё ещё приходят массивы
**Причина:** Старая версия фронтенда в кеше браузера  
**Решение:** Очистить кеш (см. Шаг 3)

### Проблема 3: Логи показывают что приходят массивы на бэкенд
**Причина:** Проблема в API клиенте  
**Решение:** Проверить что в `ordersApi.createOrder()` данные отправляются правильно

## Проверка версии кода

Убедитесь что на сервере последняя версия:
```bash
cd ~/OkoZnaniy
git log --oneline -5
```

Должны быть коммиты:
- `2ae7ce8` - debug: добавлено логирование в OrderSerializer
- `e6ddc47` - docs: добавлена быстрая инструкция
- `46c1826` - feat: добавлен скрипт автоматического заполнения справочников
- `b864a99` - docs: добавлена инструкция по решению проблемы
- `2604a07` - fix: добавлено логирование и явное приведение к числу

## Тестовый запрос через curl

Для проверки что API работает:
```bash
# Получите токен авторизации
TOKEN="ваш_токен"

# Проверьте что справочники доступны
curl -H "Authorization: Bearer $TOKEN" https://okoznaniy.ru/api/catalog/subjects/

# Попробуйте создать заказ
curl -X POST https://okoznaniy.ru/api/orders/orders/ \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Тестовый заказ",
    "description": "Описание тестового заказа",
    "deadline": "2025-01-15",
    "custom_topic": "Тестовая тема",
    "budget": 1000,
    "subject_id": 1,
    "work_type_id": 1
  }'
```

Если curl работает, но браузер нет - проблема в кеше браузера.

## Контрольный список

- [ ] Запущен `./update-and-restart.sh`
- [ ] Справочники заполнены (15+ предметов, 10+ типов работ)
- [ ] Кеш браузера очищен
- [ ] Логи фронтенда показывают `type: number`
- [ ] Логи бэкенда показывают объекты Subject/WorkType
- [ ] Тестовый заказ создаётся успешно

## Если ничего не помогло

1. Соберите все логи:
```bash
# Логи бэкенда
docker-compose logs backend --tail=200 > backend.log

# Логи nginx
docker-compose logs nginx --tail=200 > nginx.log

# Скриншот консоли браузера
```

2. Проверьте что контейнеры запущены:
```bash
docker-compose ps
```

Все должны быть в статусе `Up`.

3. Проверьте версию фронтенда в браузере:
- Откройте консоль (F12)
- Найдите строку: `🚀 Frontend Version: 2.1.0 (avatar upload fix)`
- Если версия старая - кеш не очищен

4. Перезапустите контейнеры:
```bash
docker-compose restart
```

## Ожидаемый результат

После всех исправлений:
1. ✅ Справочники заполнены
2. ✅ Фронтенд отправляет числа
3. ✅ Бэкенд получает объекты
4. ✅ Заказ создаётся успешно
5. ✅ Файлы загружаются (если прикреплены)
6. ✅ Редирект на страницу ленты заказов
