# 🔧 Не вижу новую страницу в ЛК директора - РЕШЕНИЕ

## Причина
Frontend нужно перезапустить, чтобы подхватить новые компоненты.

## ✅ БЫСТРОЕ РЕШЕНИЕ

### Вариант 1: Автоматический перезапуск

Запустите файл:
```
RESTART_FRONTEND.bat
```

### Вариант 2: Ручной перезапуск

1. Остановите frontend сервер (если запущен):
   - Нажмите `Ctrl+C` в терминале где запущен npm

2. Очистите кэш:
```bash
cd frontend-react
rmdir /s /q node_modules\.cache
```

3. Запустите снова:
```bash
npm start
```

4. Откройте браузер в режиме инкогнито или нажмите `Ctrl+Shift+R`

### Вариант 3: Жесткая перезагрузка браузера

Если frontend уже запущен:
1. Откройте http://localhost:3000/director-dashboard
2. Нажмите `Ctrl+Shift+R` (жесткая перезагрузка)
3. Или откройте в режиме инкогнито: `Ctrl+Shift+N`

## 📋 Что должно появиться

После перезапуска в левом меню ЛК директора должен появиться новый пункт:

```
📊 Управление персоналом
💰 Финансовая статистика
👥 Панель партнёров
📈 Общая статистика
💬 Коммуникация с арбитрами
🛑 Баны за обмен контактами  ← НОВЫЙ ПУНКТ
```

## 🔍 Проверка установки

Выполните команды для проверки:

```bash
# Проверить, что компонент существует
dir frontend-react\src\pages\DirectorDashboard\components\ContactBannedUsers\ContactBannedUsers.tsx

# Проверить импорт в главном файле
type frontend-react\src\pages\DirectorDashboard\DirectorDashboard.tsx | findstr "ContactBannedUsers"
```

Должно вывести:
```
import ContactBannedUsers from './components/ContactBannedUsers';
component: <ContactBannedUsers />,
```

## ⚠️ Если все еще не видно

1. **Проверьте роль пользователя**
   - Вы должны войти как admin или arbitrator
   - Проверьте в базе данных или создайте нового: `python manage.py create_admin_accounts`

2. **Проверьте консоль браузера**
   - Откройте DevTools (F12)
   - Вкладка Console
   - Ищите ошибки красного цвета

3. **Проверьте, что backend запущен**
   ```bash
   python manage.py runserver
   ```

4. **Проверьте миграции**
   ```bash
   python manage.py migrate
   ```

## 📞 Дополнительная помощь

Если проблема сохраняется:
1. Сделайте скриншот консоли браузера (F12)
2. Проверьте логи backend сервера
3. Убедитесь, что все файлы на месте (см. CHECK_INSTALLATION.md)

## ✅ Готово!

После перезапуска frontend вы должны увидеть новый раздел "Баны за обмен контактами" в меню ЛК директора.
