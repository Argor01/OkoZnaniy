# 🚀 Инструкция по деплою на Render

## ✅ Что уже сделано

1. ✅ Заменена иконка в 6-м шаге на `advantages-icon-2.svg` (молния с деньгами)
2. ✅ Уменьшен кружок логотипа:
   - Основной: 90x90px
   - Планшеты: 80x80px  
   - Мобильные: 70x70px
3. ✅ Исправлены ошибки TypeScript
4. ✅ Проект успешно собран (`npm run build`)
5. ✅ Созданы файлы для деплоя:
   - `frontend-react/render.yaml`
   - `frontend-react/.node-version`
   - `frontend-react/README_DEPLOY.md`

## 📦 Деплой на Render

### Вариант 1: Через Blueprint (рекомендуется)

1. **Загрузите проект на GitHub**
   ```bash
   git add .
   git commit -m "Ready for Render deployment"
   git push
   ```

2. **Создайте Static Site на Render**
   - Зайдите на [render.com](https://render.com)
   - Нажмите **New +** → **Static Site**
   - Подключите ваш репозиторий
   - Render автоматически обнаружит `render.yaml`

3. **Настройки (если нужно изменить)**
   - **Name**: okoznaniy-frontend
   - **Root Directory**: `frontend-react`
   - **Build Command**: `npm install && npm run build`
   - **Publish Directory**: `dist`

### Вариант 2: Ручная настройка

1. **New Static Site** на Render
2. **Подключите репозиторий**
3. **Заполните настройки**:
   ```
   Name: okoznaniy-frontend
   Root Directory: frontend-react
   Build Command: npm install && npm run build
   Publish Directory: dist
   ```

4. **Добавьте Rewrite Rule** (для SPA маршрутизации):
   ```
   Source: /*
   Destination: /index.html
   Action: Rewrite
   ```

## 🔧 Переменные окружения (если нужны)

Если у вас есть бэкенд API, добавьте в Render:

```
VITE_API_URL=https://your-backend-url.com
```

## 🌐 После деплоя

1. Render даст вам URL типа: `https://okoznaniy-frontend.onrender.com`
2. Можете добавить свой домен в настройках
3. SSL сертификат настроится автоматически

## 🧪 Локальная проверка

Перед деплоем можете проверить локально:

```bash
cd frontend-react

# Сборка
npm run build

# Предпросмотр продакшен версии
npm run preview
```

Откройте http://localhost:4173

## 📊 Размер бандла

Текущий размер после сборки:
- **CSS**: 80 KB (14 KB gzip)
- **JS**: 1.6 MB (502 KB gzip)

⚠️ Рекомендация: Рассмотрите code-splitting для уменьшения размера бандла

## 🐛 Troubleshooting

### Проблема: 404 при переходе по прямым ссылкам
**Решение**: Убедитесь что настроен rewrite rule в `render.yaml`

### Проблема: Ошибки сборки на Render
**Решение**: 
1. Проверьте логи в Render Dashboard
2. Убедитесь что `.node-version` указывает правильную версию Node.js
3. Проверьте что все зависимости в `package.json`

### Проблема: Белый экран после деплоя
**Решение**: Проверьте консоль браузера на ошибки загрузки ресурсов

## 📝 Структура файлов для деплоя

```
frontend-react/
├── dist/                    # ✅ Собранные файлы (создается при build)
├── public/                  # ✅ Статические файлы (копируются в dist)
│   └── assets/             # ✅ Иконки и изображения
├── src/                     # ✅ Исходный код
├── render.yaml             # ✅ Конфигурация Render
├── .node-version           # ✅ Версия Node.js (20.11.0)
├── package.json            # ✅ Зависимости
└── vite.config.ts          # ✅ Конфигурация Vite
```

## 🎉 Готово!

Проект полностью готов к деплою на Render. Просто загрузите на GitHub и создайте Static Site на Render.
