# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ CORS

## На хостинге выполните эти команды:

```bash
# 1. Подключитесь к серверу
ssh root@45.12.239.226

# 2. Перейдите в папку проекта
cd ~/OkoZnaniy

# 3. Получите обновления
git pull

# 4. Перезапустите проект
docker-compose down
docker-compose up -d --build

# 5. Проверьте статус
docker-compose ps

# 6. Проверьте логи (если нужно)
docker-compose logs -f backend
```

## Что было исправлено:

1. ✅ Django CORS настройки - добавлен IP `45.12.239.226`
2. ✅ Frontend API client - теперь использует переменную окружения
3. ✅ .env.production - правильный API URL

## После обновления:

Откройте http://45.12.239.226 в браузере и попробуйте авторизоваться.

CORS ошибка должна исчезнуть! ✨
