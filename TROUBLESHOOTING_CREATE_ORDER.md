# Решение проблемы создания заказа (400 Bad Request)

## Проблема
При создании заказа возникает ошибка 400 с сообщением о том, что `subject_id` и `work_type_id` являются массивами вместо чисел.

## Причины
1. **Устаревший код на сервере** - не применены изменения из коммита `9eaade5` где поля сделаны необязательными
2. **Неправильный тип данных** - фронтенд отправляет строки вместо чисел
3. **Проблема с кастомными полями** - когда выбрано "Другое", не отправляются нужные поля

## Решение

### 1. Обновить код на сервере
```bash
cd ~/OkoZnaniy
./update-and-restart.sh
```

Это применит все последние изменения:
- Коммит `9eaade5`: поля `subject_id` и `work_type_id` сделаны необязательными
- Коммит `2604a07`: добавлено явное приведение к числу и логирование

### 2. Проверить логи после обновления

После обновления попробуйте создать заказ и проверьте консоль браузера:
```
📝 Отправка заказа: {...}
🔍 showCustomSubject: false
🔍 showCustomWorkType: false
🔍 values.subject_id type: number 1
🔍 values.work_type_id type: number 3
📤 Данные для отправки: {...}
📤 Типы полей: { subject_id: 'number', work_type_id: 'number' }
```

### 3. Проверить миграции БД

Убедитесь что все миграции применены:
```bash
docker-compose exec backend python manage.py showmigrations
```

Если есть неприменённые миграции:
```bash
docker-compose exec backend python manage.py migrate
```

## Что было исправлено

### Backend (apps/orders/serializers.py)
```python
# Поля теперь необязательные
subject_id = serializers.PrimaryKeyRelatedField(
    source='subject', 
    write_only=True, 
    queryset=Subject.objects.all(), 
    required=False,  # ← Добавлено
    allow_null=True  # ← Добавлено
)

work_type_id = serializers.PrimaryKeyRelatedField(
    source='work_type', 
    write_only=True, 
    queryset=WorkType.objects.all(), 
    required=False,  # ← Добавлено
    allow_null=True  # ← Добавлено
)
```

### Валидация
```python
def validate(self, data):
    # Проверяем что указан либо ID либо кастомное значение
    if not data.get('subject') and not data.get('custom_subject'):
        raise serializers.ValidationError({
            'subject_id': 'Укажите предмет из списка или введите произвольный предмет'
        })
    
    if not data.get('work_type') and not data.get('custom_work_type'):
        raise serializers.ValidationError({
            'work_type_id': 'Укажите тип работы из списка или введите произвольный тип'
        })
```

### Frontend (CreateOrder/index.tsx)
```typescript
// Явное приведение к числу
if (!showCustomSubject && values.subject_id) {
  orderData.subject_id = Number(values.subject_id);
}
if (!showCustomWorkType && values.work_type_id) {
  orderData.work_type_id = Number(values.work_type_id);
}
```

## Тестирование

### Сценарий 1: Создание заказа с предметом из списка
1. Выберите предмет из списка (например, "Математика")
2. Выберите тип работы из списка (например, "Курсовая работа")
3. Заполните остальные поля
4. Нажмите "Создать заказ"
5. **Ожидаемый результат**: Заказ создан успешно

### Сценарий 2: Создание заказа с кастомным предметом
1. Выберите "Другое" в списке предметов
2. Введите свой предмет (например, "Квантовая физика")
3. Выберите тип работы из списка
4. Заполните остальные поля
5. Нажмите "Создать заказ"
6. **Ожидаемый результат**: Заказ создан, предмет автоматически добавлен в справочник

### Сценарий 3: Создание заказа с кастомным типом работы
1. Выберите предмет из списка
2. Выберите "Другое" в типе работы
3. Введите свой тип (например, "Научная статья")
4. Заполните остальные поля
5. Нажмите "Создать заказ"
6. **Ожидаемый результат**: Заказ создан, тип работы добавлен в справочник

## Проверка на сервере

### Проверить версию кода
```bash
cd ~/OkoZnaniy
git log --oneline -5
```

Должны быть коммиты:
- `2604a07` - fix: добавлено логирование и явное приведение subject_id и work_type_id к числу
- `8580247` - fix: возвращать полный URL для аватарок вместо относительного пути
- `1ba68a2` - docs: добавлена документация и тест для уведомлений о статусе заявки эксперта
- `9eaade5` - fix: сделать subject_id и work_type_id необязательными, добавить поддержку кастомных значений

### Проверить логи Django
```bash
docker-compose logs backend --tail=50
```

Ищите ошибки валидации:
```
ValidationError: {'subject_id': [...]}
ValidationError: {'work_type_id': [...]}
```

### Проверить логи Nginx
```bash
docker-compose logs nginx --tail=50
```

Ищите 400 ошибки:
```
POST /api/orders/orders/ HTTP/1.1" 400
```

## Дополнительная отладка

### Включить DEBUG режим (только для тестирования!)
В `.env` файле:
```bash
DEBUG=True
```

Перезапустить:
```bash
docker-compose restart backend
```

### Проверить через curl
```bash
curl -X POST https://okoznaniy.ru/api/orders/orders/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Тестовый заказ",
    "description": "Описание",
    "deadline": "2025-01-01",
    "custom_topic": "Тема",
    "budget": 1000,
    "subject_id": 1,
    "work_type_id": 1
  }'
```

### Проверить через Django shell
```bash
docker-compose exec backend python manage.py shell
```

```python
from apps.orders.models import Order
from apps.catalog.models import Subject, WorkType
from django.contrib.auth import get_user_model

User = get_user_model()

# Проверить наличие предметов и типов работ
print("Subjects:", Subject.objects.count())
print("WorkTypes:", WorkType.objects.count())

# Попробовать создать заказ вручную
user = User.objects.first()
subject = Subject.objects.first()
work_type = WorkType.objects.first()

order = Order.objects.create(
    client=user,
    title="Тест",
    description="Описание",
    deadline="2025-01-01",
    budget=1000,
    subject=subject,
    work_type=work_type,
    custom_topic="Тема"
)
print("Order created:", order.id)
```

## Частые ошибки

### Ошибка: "subject_id: This field is required"
**Причина**: Старая версия кода на сервере  
**Решение**: Запустить `./update-and-restart.sh`

### Ошибка: "subject_id: Incorrect type. Expected pk value, received list"
**Причина**: Фронтенд отправляет массив вместо числа  
**Решение**: Обновить фронтенд код (уже исправлено в коммите `2604a07`)

### Ошибка: "custom_topic: This field is required"
**Причина**: Не указана тема  
**Решение**: Убедиться что поле "Тема" заполнено

### Ошибка: "deadline: Дедлайн не может быть в прошлом"
**Причина**: Выбрана дата в прошлом  
**Решение**: Выбрать дату в будущем

## Контакты для поддержки

Если проблема не решена:
1. Проверьте все шаги выше
2. Соберите логи (браузер + backend + nginx)
3. Сделайте скриншот ошибки
4. Опишите шаги воспроизведения
