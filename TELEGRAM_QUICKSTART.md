# ⚡ Быстрый старт: Telegram авторизация

## 🎯 Цель

Запустить Telegram Login Widget для локального тестирования.

## 📋 Что нужно

1. ✅ Telegram бот создан (у вас уже есть: `@oko_expert_bot`)
2. ✅ Backend запущен и работает
3. ⚠️ Ngrok authtoken (получить ниже)

## 🚀 Шаги (5 минут)

### 1. Получите Ngrok токен

1. Откройте: https://dashboard.ngrok.com/signup
2. Зарегистрируйтесь (можно через GitHub)
3. Скопируйте authtoken: https://dashboard.ngrok.com/get-started/your-authtoken

### 2. Добавьте токен в .env

Откройте `.env` и замените:

```env
NGROK_AUTHTOKEN=your_ngrok_authtoken_here
```

На ваш токен:

```env
NGROK_AUTHTOKEN=2abc123def456ghi789jkl0mno1pqr2stu3vwx4yz5
```

### 3. Запустите ngrok

```bash
docker-compose --profile dev up -d ngrok
```

### 4. Получите публичный URL

Откройте в браузере: http://localhost:4040

Или в консоли:

```bash
docker-compose logs ngrok | findstr "url="
```

Скопируйте URL для frontend (например: `https://abc123.ngrok.io`)

### 5. Настройте домен в BotFather

1. Откройте Telegram
2. Найдите: [@BotFather](https://t.me/BotFather)
3. Отправьте: `/setdomain`
4. Выберите: `@oko_expert_bot`
5. Введите домен БЕЗ `https://`: `abc123.ngrok.io`

### 6. Обновите CORS

Откройте `config/settings.py` и добавьте ваш ngrok URL:

```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "https://abc123.ngrok.io",  # ← Ваш ngrok URL
]
```

Сохраните и перезапустите backend:

```bash
docker-compose restart backend
```

### 7. Откройте приложение

Откройте в браузере ваш ngrok URL: `https://abc123.ngrok.io`

Перейдите на страницу логина: `https://abc123.ngrok.io/login`

### 8. Тестируйте!

Вы должны увидеть синюю кнопку "Login with Telegram".

Нажмите на неё → Подтвердите в Telegram → Готово! 🎉

## 🐛 Проблемы?

### Кнопка не появляется

1. Откройте консоль браузера (F12)
2. Проверьте ошибки
3. Убедитесь, что открыли ngrok URL, а не localhost

### "Bot domain invalid"

1. Проверьте, что домен добавлен в BotFather
2. Домен должен быть БЕЗ `https://` и БЕЗ пути
3. Правильно: `abc123.ngrok.io`
4. Неправильно: `https://abc123.ngrok.io/login`

### CORS ошибки

1. Проверьте, что ngrok URL добавлен в `CORS_ALLOWED_ORIGINS`
2. Перезапустите backend: `docker-compose restart backend`

### Ngrok не запускается

1. Проверьте authtoken в `.env`
2. Посмотрите логи: `docker-compose logs ngrok`

## 📚 Подробные инструкции

- [NGROK_DOCKER.md](./NGROK_DOCKER.md) - Полная документация по ngrok
- [TELEGRAM_SETUP_BOTFATHER.md](./TELEGRAM_SETUP_BOTFATHER.md) - Настройка бота
- [TELEGRAM_AUTH_README.md](./TELEGRAM_AUTH_README.md) - Как работает авторизация

## 💡 Полезно знать

- **Web UI ngrok:** http://localhost:4040 - просмотр всех запросов
- **Остановить ngrok:** `docker-compose stop ngrok`
- **Перезапустить ngrok:** `docker-compose restart ngrok`
- **Новый URL:** При каждом перезапуске ngrok генерирует новый URL (бесплатный план)

## 🎓 Для продакшена

На продакшене ngrok не нужен! Просто укажите ваш реальный домен в BotFather:

```
/setdomain
@oko_expert_bot
yourdomain.com
```

И виджет будет работать автоматически.

---

**Готово! Теперь вы можете тестировать Telegram авторизацию локально.** 🚀
