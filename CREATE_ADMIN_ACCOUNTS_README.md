# Создание тестовых аккаунтов администраторов

Этот набор скриптов создает тестовые аккаунты для администрирования системы.

## Создаваемые аккаунты

### 📧 Директор
- **Email:** director@test.com
- **Пароль:** test123
- **Роль:** admin (с правами директора)
- **Права:** Полный доступ к системе, включая панель директора

### 🤝 Партнер
- **Email:** partner@test.com
- **Пароль:** test123
- **Роль:** partner
- **Особенности:** 
  - Баланс: 5000 ₽
  - Комиссия: 10%
  - Автоматически генерируется реферальный код

### ⚙️ Администратор
- **Email:** admin@test.com
- **Пароль:** test123
- **Роль:** admin
- **Права:** Полный доступ к админ-панели Django

## Способы запуска

### 1. Windows (рекомендуется)
```cmd
create_admin_accounts.bat
```

### 2. Linux/macOS
```bash
chmod +x create_admin_accounts.sh
./create_admin_accounts.sh
```

### 3. Прямой вызов Django команды
```bash
python manage.py create_admin_accounts
```

### 4. Прямой запуск Python скрипта
```bash
python create_admin_users.py
```

## Требования

1. **База данных должна быть запущена**
   ```bash
   docker-compose -f docker-compose.dev.yml up postgres redis -d
   ```

2. **Выполнены миграции**
   ```bash
   python manage.py migrate
   ```

3. **Установлены зависимости Python**
   ```bash
   pip install -r requirements.txt
   ```

## Особенности

- Скрипт безопасно обновляет существующих пользователей, если они уже созданы
- Для партнера автоматически генерируется уникальный реферальный код
- Все пароли устанавливаются в зашифрованном виде через Django
- Email адреса помечаются как подтвержденные

## Использование после создания

После создания аккаунтов вы можете:

1. **Войти в систему** используя любой из созданных аккаунтов
2. **Тестировать функционал** различных ролей
3. **Разрабатывать новые функции** с правильными правами доступа

## Безопасность

⚠️ **Важно:** Эти аккаунты предназначены только для разработки и тестирования. 
Не используйте их в продакшене!

В продакшене:
- Измените пароли на сложные
- Используйте реальные email адреса
- Настройте двухфакторную аутентификацию
- Ограничьте права доступа по необходимости