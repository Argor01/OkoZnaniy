# Информация о портах

## ⚠️ ВАЖНО: Используйте правильный порт!

### Запущенные серверы:

| Сервер | Порт | URL | Назначение |
|--------|------|-----|------------|
| **React (Vite)** | **5173** | **http://localhost:5173/** | **Frontend - ИСПОЛЬЗУЙТЕ ЭТОТ!** |
| Django | 3000 | http://localhost:3000/ | Backend API |
| Django | 8000 | http://localhost:8000/ | Backend API (альтернативный) |

## 🎯 Правильный URL для тестирования

### ✅ ПРАВИЛЬНО:
```
http://localhost:5173/
http://localhost:5173/dashboard
http://localhost:5173/expert
http://localhost:5173/login
```

### ❌ НЕПРАВИЛЬНО:
```
http://localhost:3000/dashboard  ← Это Django, не React!
http://localhost:8000/dashboard  ← Это тоже Django!
```

## Почему порт 3000 показывает "не то"?

**Порт 3000** - это Django backend. Он не знает о React маршрутах и показывает либо:
- 404 ошибку
- Django admin
- API endpoints
- Или старую версию frontend (если она там есть)

## Как проверить, что открыт правильный порт?

### Способ 1: Посмотрите на URL в браузере
```
✅ localhost:5173  ← Правильно (React)
❌ localhost:3000  ← Неправильно (Django)
❌ localhost:8000  ← Неправильно (Django)
```

### Способ 2: Откройте DevTools (F12)
В консоли должно быть:
```
[vite] connected.
```

Если видите это - вы на правильном порту!

### Способ 3: Проверьте заголовок страницы
React приложение должно показывать ваш заголовок из `index.html`

## Что делать, если порт 5173 не работает?

### 1. Проверьте, запущен ли dev-сервер
```bash
cd frontend-react
npm run dev
```

Должно появиться:
```
VITE v7.1.7  ready in XXX ms
➜  Local:   http://localhost:5173/
```

### 2. Проверьте, не занят ли порт
```powershell
netstat -ano | Select-String ":5173"
```

Должна быть строка с `LISTENING`

### 3. Перезапустите dev-сервер
```bash
# Остановите (Ctrl+C)
# Запустите снова
npm run dev
```

## Структура проекта

```
OkoZnaniy/
├── frontend-react/          ← React приложение
│   ├── src/
│   ├── package.json
│   └── vite.config.ts      ← Настройки Vite (порт 5173)
│
├── backend/                 ← Django приложение
│   ├── manage.py
│   └── settings.py         ← Настройки Django (порт 3000/8000)
│
└── ...
```

## Быстрая проверка

Откройте в браузере:

1. **http://localhost:5173/** ← React (должен работать)
2. **http://localhost:3000/api/** ← Django API (должен работать)
3. **http://localhost:8000/api/** ← Django API (должен работать)

## Закладки для браузера

Добавьте эти закладки для быстрого доступа:

- 🎨 **React App:** http://localhost:5173/
- 🔧 **Django API:** http://localhost:8000/api/
- 👤 **Django Admin:** http://localhost:8000/admin/

## Если все еще не работает

1. **Очистите кэш браузера** (Ctrl+Shift+Delete)
2. **Сделайте Hard Reload** (Ctrl+Shift+R)
3. **Откройте в режиме инкогнито** (Ctrl+Shift+N)
4. **Проверьте консоль браузера** (F12) на ошибки
5. **Проверьте терминал** где запущен `npm run dev`

## Итог

### 🎯 Используйте этот URL:
```
http://localhost:5173/
```

### ❌ НЕ используйте:
```
http://localhost:3000/  ← Django backend
http://localhost:8000/  ← Django backend
```

---

**Запомните:** 
- **5173** = React (Frontend) ✅
- **3000/8000** = Django (Backend) ❌
