# Быстрый тест претензий на фронтенде

## 🚀 За 5 минут

### 1. Подготовка (1 мин)
```bash
# Запустите приложение
docker-compose up -d

# Создайте тестовых пользователей (если нужно)
docker-compose exec backend python test_claim_to_admin_flow.py
```

### 2. Тест как эксперт (2 мин)

**Войдите:** http://localhost:5173
- Username: `test_expert`
- Password: `test123`

**Создайте претензию:**
1. Откройте любой чат
2. Нажмите "Подать претензию" (красная кнопка/иконка)
3. Заполните:
   - Тип: "Качество работы"
   - Тема: "Тест претензии"
   - Описание: "Проверка системы"
4. Нажмите "Отправить"

**✅ Проверьте:**
- Появилось сообщение об успехе
- Нет ошибок в консоли (F12)

### 3. Тест как администратор (2 мин)

**Выйдите и войдите:**
- Username: `test_admin`
- Password: `test123`

**Проверьте претензию:**
1. Откройте "Админ-панель"
2. Перейдите в "Новые обращения"
3. Найдите претензию "Тест претензии"

**✅ Проверьте:**
- Претензия отображается
- Все данные корректны
- Можно открыть детали

**Обработайте:**
1. Нажмите "Взять в работу"
2. Перейдите в "Обращения в работе"
3. Найдите претензию
4. Нажмите "Завершить"
5. Введите решение: "Проблема решена"
6. Подтвердите

**✅ Проверьте:**
- Претензия переместилась в "Завершенные"
- Статусы меняются корректно

---

## 🎯 Что должно работать

### Создание (Эксперт)
- ✅ Форма открывается
- ✅ Претензия создается
- ✅ Уведомление об успехе

### Просмотр (Администратор)
- ✅ Претензия в "Новые обращения"
- ✅ Все данные видны
- ✅ Можно открыть детали

### Обработка (Администратор)
- ✅ "Взять в работу" работает
- ✅ Претензия перемещается в "В работе"
- ✅ "Завершить" работает
- ✅ Претензия перемещается в "Завершенные"

---

## 🔍 Быстрая проверка через DevTools

**Откройте консоль (F12):**

### При создании претензии
```
POST /api/admin-panel/claims/ → 201 Created
```

### При просмотре списка
```
GET /api/admin-panel/claims/?status=new → 200 OK
```

### При взятии в работу
```
POST /api/admin-panel/claims/1/take_in_work/ → 200 OK
```

### При завершении
```
POST /api/admin-panel/claims/1/complete_claim/ → 200 OK
```

**❌ НЕ должно быть:**
- Ошибок 404, 500
- Красных ошибок в консоли
- CORS ошибок

---

## 🐛 Если не работает

### Претензия не создается
```bash
# Проверьте логи
docker-compose logs backend

# Проверьте роль пользователя
docker-compose exec backend python manage.py shell
>>> from django.contrib.auth import get_user_model
>>> User = get_user_model()
>>> user = User.objects.get(username='test_expert')
>>> print(user.role)  # Должно быть 'expert'
```

### Претензия не видна администратору
```bash
# Проверьте, что претензия создалась
docker-compose exec backend python test_claims_creation.py

# Проверьте роль администратора
docker-compose exec backend python manage.py shell
>>> admin = User.objects.get(username='test_admin')
>>> print(admin.role)  # Должно быть 'admin'
```

### Кнопки не работают
1. Обновите страницу (F5)
2. Очистите кэш (Ctrl+Shift+R)
3. Проверьте консоль на ошибки (F12)

---

## 📋 Минимальный чек-лист

- [ ] Эксперт может создать претензию
- [ ] Претензия появляется в админ-панели
- [ ] Администратор может взять в работу
- [ ] Администратор может завершить
- [ ] Нет ошибок в консоли
- [ ] Все API запросы возвращают 200/201

**Если все пункты ✅ - система работает!**

---

## 📚 Подробная инструкция

Для детального тестирования смотрите:
- `ТЕСТ_ПРЕТЕНЗИЙ_ФРОНТЕНД.md` - полная инструкция
- `ТЕСТ_ПРЕТЕНЗИЙ.md` - тестирование бэкенда
- `HOW_TO_TEST_CLAIM_FLOW.md` - подробное руководство
