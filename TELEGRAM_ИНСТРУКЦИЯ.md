# 📱 Авторизация через Telegram - Инструкция

## ✅ Что сделано

Полностью реализована авторизация через Telegram бота для вашей платформы.

## 🎯 Как это работает

1. Пользователь нажимает кнопку "Войти через Telegram"
2. Открывается окно Telegram с подтверждением
3. После подтверждения пользователь автоматически авторизуется
4. Создается аккаунт (если новый пользователь) или выполняется вход

## 🚀 Настройка (5 минут)

### Шаг 1: Настройте бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду: `/setdomain`
3. Выберите вашего бота: `@oko_expert_bot`
4. Введите домен: `localhost` (для разработки) или ваш реальный домен

**Пример:**
```
/setdomain
@oko_expert_bot
localhost
```

### Шаг 2: Запустите проект

**Через Docker (рекомендуется):**
```bash
docker-compose up -d
```

**Или локально:**
```bash
# Терминал 1 - Backend
python manage.py runserver

# Терминал 2 - Frontend
cd frontend-react
npm run dev
```

### Шаг 3: Добавьте кнопку на страницу

Откройте вашу страницу логина и добавьте:

```tsx
import TelegramLoginButton from '../components/auth/TelegramLoginButton';

// В вашем компоненте
<TelegramLoginButton
  botName="oko_expert_bot"
  onAuth={(user) => {
    // Пользователь успешно авторизован
    console.log('Авторизован:', user);
    // Перенаправьте куда нужно
    navigate('/dashboard');
  }}
  onError={(error) => {
    // Произошла ошибка
    alert('Ошибка: ' + error);
  }}
/>
```

## 📝 Готовые файлы

### Backend (Django)
- ✅ `apps/users/telegram_auth.py` - Логика авторизации
- ✅ `apps/users/views.py` - API endpoint добавлен
- ✅ `.env` - Токен бота добавлен
- ✅ `config/settings.py` - Настройки добавлены

### Frontend (React)
- ✅ `frontend-react/src/components/auth/TelegramLoginButton.tsx` - Готовый компонент
- ✅ `frontend-react/src/pages/TelegramAuthExample.tsx` - Пример страницы

## 🧪 Тестирование

### Вариант 1: Через браузер
1. Откройте `http://localhost:3000`
2. Найдите кнопку Telegram
3. Нажмите и авторизуйтесь

### Вариант 2: Через тестовый скрипт
```bash
python test_telegram_auth.py
```

## 🎨 Настройка внешнего вида

```tsx
<TelegramLoginButton
  botName="oko_expert_bot"
  buttonSize="large"        // large, medium, small
  cornerRadius={10}         // Радиус углов (0-20)
  usePic={true}            // Показывать аватар
  lang="ru"                // Язык: ru, en, uk и т.д.
  onAuth={handleAuth}
  onError={handleError}
/>
```

## 🔐 Безопасность

- ✅ Проверка подлинности данных через HMAC-SHA256
- ✅ Проверка времени (данные не старше 24 часов)
- ✅ JWT токены для безопасного доступа к API
- ✅ Токен бота хранится только на сервере

## 📊 API Endpoint

```
POST /api/users/telegram_auth/
```

**Что возвращает:**
```json
{
  "access": "JWT токен для API",
  "refresh": "JWT токен для обновления",
  "user": {
    "id": 1,
    "username": "username",
    "telegram_id": 123456789,
    "role": "client",
    ...
  }
}
```

## 🐛 Если что-то не работает

### Кнопка не появляется
- Проверьте, что `botName="oko_expert_bot"` указан правильно
- Откройте консоль браузера (F12) и посмотрите ошибки
- Убедитесь, что домен добавлен в BotFather

### Ошибка "Bot domain invalid"
- Добавьте домен в BotFather через `/setdomain`
- Для локальной разработки используйте `localhost`

### Ошибка "Invalid hash"
- Проверьте, что токен бота правильный в `.env`
- Перезапустите backend

### Пользователь не создается
- Проверьте логи: `docker-compose logs backend`
- Убедитесь, что база данных запущена
- Проверьте, что миграции применены

## 📱 Для продакшена

1. Замените `localhost` на ваш реальный домен в BotFather:
```
/setdomain
@oko_expert_bot
yourdomain.com
```

2. Обновите `ALLOWED_HOSTS` в `config/settings.py`

3. Настройте HTTPS (обязательно для продакшена)

## 💡 Дополнительные возможности

### Привязка Telegram к существующему аккаунту

Можно добавить функцию привязки Telegram к уже существующему аккаунту. Пользователь сначала входит обычным способом, затем привязывает Telegram.

### Уведомления через бота

В будущем можно добавить отправку уведомлений пользователям через Telegram бота.

## 📚 Полная документация

- [TELEGRAM_AUTH_QUICKSTART.md](./TELEGRAM_AUTH_QUICKSTART.md) - Быстрый старт
- [TELEGRAM_AUTH_SETUP.md](./TELEGRAM_AUTH_SETUP.md) - Подробная настройка
- [TELEGRAM_AUTH_README.md](./TELEGRAM_AUTH_README.md) - Техническая документация

## ✨ Готово!

Авторизация через Telegram полностью настроена и готова к использованию. Просто добавьте компонент `TelegramLoginButton` на вашу страницу логина.

**Токен бота:** `8584999235:AAGKcP0nhnn_B6G8iTa2Ti8U9oxUFByWfpo`
**Username бота:** `@oko_expert_bot`

Если возникнут вопросы - смотрите документацию выше или проверьте логи.
