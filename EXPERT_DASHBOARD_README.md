# Expert Dashboard Backend API

Бэкенд API для личного кабинета эксперта платформы OkoZnaniy.

## 🚀 Что реализовано

### ✅ Модели и База данных
- Обновлена модель `Notification` с полем `data` (JSONField)
- Добавлены новые типы уведомлений: `ORDER_ASSIGNED`, `APPLICATION_APPROVED`, `APPLICATION_REJECTED`
- Добавлены индексы для оптимизации:
  - `Order`: (expert, status, created_at), (status, subject, created_at)
  - `Transaction`: (user, timestamp), (user, type, timestamp)
  - `ExpertReview`: (expert, is_published, created_at)

### ✅ Слой сервисов
- **ExpertStatisticsService** - статистика с кэшированием (5 мин)
  - `get_dashboard_statistics()` - агрегированная статистика
  - `get_earnings_by_period()` - заработок за период
  - `update_expert_statistics()` - обновление статистики

- **ExpertFinanceService** - финансовая информация
  - `get_financial_summary()` - финансовая сводка
  - `get_transactions()` - транзакции с фильтрацией
  - `calculate_pending_payouts()` - ожидающие выплаты
  - `get_last_payout()` - последняя выплата

- **ExpertOrderService** - управление заказами
  - `get_available_orders()` - доступные заказы с оптимизацией
  - `get_active_orders()` - активные заказы
  - `can_take_order()` - проверка возможности взятия заказа
  - `take_order()` - взятие заказа в работу

- **ExpertCacheService** - управление кэшем
  - `invalidate_expert_cache()` - инвалидация всего кэша
  - `invalidate_statistics_cache()` - инвалидация кэша статистики
  - `invalidate_profile_cache()` - инвалидация кэша профиля

### ✅ API Эндпоинты

Все эндпоинты доступны по базовому URL: `/api/experts/dashboard/`

#### Статистика и профиль
- `GET /statistics/` - Получить статистику эксперта
- `GET /profile/` - Получить профиль эксперта (с кэшированием 10 мин)

#### Финансы
- `GET /financial-summary/` - Финансовая сводка (баланс, заработок, выплаты)
- `GET /transactions/` - Список транзакций с фильтрацией
  - Query params: `type`, `date_from`, `date_to`, `page`, `page_size`

#### Заказы
- `GET /active-orders/` - Активные заказы эксперта
- `GET /available-orders/` - Доступные заказы по специализациям
- `GET /recent-orders/` - История заказов (последние 10)
- `POST /take-order/` - Взять заказ в работу
  - Body: `{"order_id": 123}`

#### Отзывы
- `GET /reviews/` - Отзывы о работе эксперта
  - Query params: `rating`, `page`, `page_size`
  - Response включает: `average_rating`, `rating_distribution`

#### Уведомления
- `GET /notifications/` - Список уведомлений
  - Query params: `unread_only`, `type`, `page`, `page_size`
  - Response включает: `unread_count`
- `POST /{id}/mark-read/` - Отметить уведомление как прочитанное

### ✅ Сериализаторы
- `TransactionSerializer` - транзакции с информацией о заказе
- `NotificationSerializer` - уведомления с типом
- `ExpertReviewDetailSerializer` - детальные отзывы

### ✅ Оптимизация
- Кэширование статистики (5 минут)
- Кэширование профиля (10 минут)
- Оптимизация запросов через `select_related` и `prefetch_related`
- Индексы базы данных для частых запросов

### ✅ Безопасность и логирование
- Проверка роли пользователя (только эксперты)
- Обработка ошибок с детальными сообщениями
- Логирование важных операций
- Валидация входных данных

## 📋 Быстрый старт

### 1. Применить миграции
```bash
cd OkoZnaniy
python manage.py migrate
```

### 2. Создать тестового эксперта
```bash
python create_test_expert.py
```

Или через shell:
```bash
python manage.py shell
```
```python
from apps.users.models import User
u, created = User.objects.get_or_create(
    username='expert_test',
    defaults={'email': 'expert@test.com', 'role': 'expert'}
)
u.set_password('test123')
u.save()
```

### 3. Запустить сервер
```bash
python manage.py runserver
```

### 4. Получить токен авторизации

**Через браузер:**
Откройте `http://127.0.0.1:8000/api/users/token/`

Введите:
- Username: `expert_test`
- Password: `test123`

**Через curl/PowerShell:**
```powershell
$body = '{"username":"expert_test","password":"test123"}'
$response = Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/users/token/" `
    -Method Post -Body $body -ContentType "application/json"
$token = $response.access
```

### 5. Тестировать API

**Через браузер (Django REST Framework UI):**
1. Откройте любой эндпоинт, например: `http://127.0.0.1:8000/api/experts/dashboard/statistics/`
2. Нажмите "Log in" в правом верхнем углу
3. Войдите с учетными данными эксперта
4. Тестируйте эндпоинты прямо в браузере

**Через HTML интерфейс:**
Откройте файл `test_api.html` в браузере

**Через PowerShell:**
```powershell
# Запустить тестовый скрипт
./test_api.ps1

# Или вручную
$headers = @{
    "Authorization" = "Bearer $token"
    "Content-Type" = "application/json"
}

# Статистика
Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/experts/dashboard/statistics/" `
    -Method Get -Headers $headers

# Финансовая сводка
Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/experts/dashboard/financial-summary/" `
    -Method Get -Headers $headers

# Транзакции с фильтрами
Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/experts/dashboard/transactions/?type=payout" `
    -Method Get -Headers $headers
```

## 📊 Примеры ответов API

### GET /statistics/
```json
{
  "total_earnings": 150000.00,
  "monthly_earnings": 25000.00,
  "active_orders": 3,
  "completed_orders": 45,
  "average_rating": 4.7,
  "verified_specializations": 5,
  "success_rate": 95.5,
  "total_orders": 48,
  "response_time_avg": 3600.0
}
```

### GET /financial-summary/
```json
{
  "current_balance": 15000.00,
  "frozen_balance": 5000.00,
  "available_balance": 10000.00,
  "total_earnings": 150000.00,
  "monthly_earnings": 25000.00,
  "pending_payouts": 5000.00,
  "last_payout": {
    "amount": 10000.00,
    "date": "2025-11-01T10:00:00Z"
  }
}
```

### GET /reviews/
```json
{
  "count": 45,
  "average_rating": 4.7,
  "rating_distribution": {
    "5": 30,
    "4": 10,
    "3": 3,
    "2": 1,
    "1": 1
  },
  "results": [
    {
      "id": 1,
      "rating": 5,
      "comment": "Отличная работа!",
      "client": {
        "id": 10,
        "username": "client123"
      },
      "order": {
        "id": 123,
        "title": "Курсовая работа"
      },
      "created_at": "2025-11-05T12:00:00Z"
    }
  ]
}
```

## 🔧 Конфигурация

### Настройки кэширования
В `settings.py` настроен Redis для кэширования:
```python
CACHES = {
    'default': {
        'BACKEND': 'django_redis.cache.RedisCache',
        'LOCATION': 'redis://127.0.0.1:6379/1',
        'OPTIONS': {
            'CLIENT_CLASS': 'django_redis.client.DefaultClient',
        }
    }
}
```

### Таймауты кэша
- Статистика: 300 секунд (5 минут)
- Профиль: 600 секунд (10 минут)

## 📚 Документация

Полная документация находится в папке `.kiro/specs/expert-dashboard-enhancement/`:
- `requirements.md` - Требования (EARS-формат)
- `design.md` - Архитектура и дизайн
- `tasks.md` - План реализации

## 🧪 Тестирование

### Учетные данные для тестирования
- **Username:** `expert_test`
- **Password:** `test123`

### Инструменты тестирования
1. **test_api.html** - Веб-интерфейс для тестирования
2. **test_api.ps1** - PowerShell скрипт для автоматического тестирования
3. **Django REST Framework UI** - Встроенный интерфейс в браузере

## 🐛 Отладка

### Просмотр логов
Логи выводятся в консоль при запуске сервера. Важные операции логируются:
- Попытки входа
- Взятие заказов
- Ошибки при получении данных

### Проверка кэша
```python
from django.core.cache import cache

# Проверить кэш статистики
stats = cache.get('expert_dashboard_stats_1')

# Очистить кэш
cache.delete('expert_dashboard_stats_1')
```

### Обновление статистики вручную
```python
from apps.experts.services import ExpertStatisticsService
from apps.users.models import User

expert = User.objects.get(username='expert_test')
ExpertStatisticsService.update_expert_statistics(expert)
```

## 📝 Changelog

### Version 1.0.0 (2025-11-12)
- ✅ Реализован полный API для личного кабинета эксперта
- ✅ Добавлено кэширование для оптимизации
- ✅ Добавлены индексы базы данных
- ✅ Реализована система уведомлений
- ✅ Добавлено логирование и обработка ошибок
- ✅ Созданы инструменты для тестирования

## 🤝 Contributing

При внесении изменений:
1. Обновите соответствующие тесты
2. Обновите документацию
3. Проверьте работу кэширования
4. Убедитесь, что все миграции применены

## 📞 Support

При возникновении проблем:
1. Проверьте логи сервера
2. Убедитесь, что миграции применены
3. Проверьте, что Redis запущен (для кэширования)
4. Проверьте права доступа пользователя (роль должна быть 'expert')
