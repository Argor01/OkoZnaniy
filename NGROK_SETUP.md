# 🌐 Настройка ngrok для локальной разработки

## Зачем нужен ngrok?

Telegram Login Widget работает только с публичными доменами. Ngrok создает публичный URL для вашего localhost, позволяя тестировать Telegram авторизацию локально.

## 🐳 Рекомендуется: Ngrok через Docker

**Самый простой способ!** Ngrok уже интегрирован в docker-compose.

👉 **Смотрите подробную инструкцию: [NGROK_DOCKER.md](./NGROK_DOCKER.md)**

Быстрый старт:
```bash
# 1. Добавьте NGROK_AUTHTOKEN в .env
# 2. Запустите
docker-compose --profile dev up -d ngrok
# 3. Откройте http://localhost:4040 для получения URL
```

---

## Альтернатива: Установка ngrok локально

Если не хотите использовать Docker, можете установить ngrok локально:

## 📥 Установка

### Шаг 1: Скачайте ngrok

1. Перейдите на https://ngrok.com/download
2. Скачайте версию для Windows
3. Распакуйте `ngrok.exe` в удобную папку (например, `C:\ngrok\`)

### Шаг 2: Зарегистрируйтесь

1. Создайте аккаунт на https://dashboard.ngrok.com/signup
2. Получите authtoken на https://dashboard.ngrok.com/get-started/your-authtoken

### Шаг 3: Настройте authtoken

Откройте PowerShell и выполните:

```powershell
cd C:\ngrok
.\ngrok config add-authtoken YOUR_AUTH_TOKEN
```

Замените `YOUR_AUTH_TOKEN` на ваш токен.

## 🚀 Использование

### Запуск для frontend (порт 3000)

```powershell
cd C:\ngrok
.\ngrok http 3000
```

### Запуск для backend (порт 8000)

```powershell
cd C:\ngrok
.\ngrok http 8000
```

### Что вы увидите

```
ngrok                                                                           
                                                                                
Session Status                online                                            
Account                       your@email.com (Plan: Free)                      
Version                       3.x.x                                            
Region                        United States (us)                               
Latency                       45ms                                             
Web Interface                 http://127.0.0.1:4040                            
Forwarding                    https://abc123.ngrok.io -> http://localhost:3000 

Connections                   ttl     opn     rt1     rt5     p50     p90      
                              0       0       0.00    0.00    0.00    0.00     
```

**Ваш публичный URL:** `https://abc123.ngrok.io`

## ⚙️ Настройка проекта

### 1. Настройте домен в BotFather

```
/setdomain
@oko_expert_bot
abc123.ngrok.io
```

⚠️ **Важно:** Указывайте домен БЕЗ `https://`

### 2. Обновите CORS в Django

Добавьте ngrok URL в `config/settings.py`:

```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "https://abc123.ngrok.io",  # Ваш ngrok URL
]
```

### 3. Перезапустите backend

```bash
docker-compose restart backend
```

### 4. Откройте приложение через ngrok

Вместо `http://localhost:3000` используйте `https://abc123.ngrok.io`

## 🎯 Тестирование Telegram авторизации

1. Откройте `https://abc123.ngrok.io/login`
2. Увидите кнопку "Login with Telegram"
3. Нажмите на неё
4. Подтвердите в Telegram
5. Готово! Вы авторизованы

## 💡 Полезные советы

### Фиксированный поддомен (Платная версия)

Бесплатная версия ngrok генерирует случайный URL при каждом запуске. Платная версия позволяет использовать фиксированный поддомен:

```bash
ngrok http 3000 --subdomain=myapp
```

Тогда URL всегда будет `https://myapp.ngrok.io`

### Web Interface

Ngrok предоставляет веб-интерфейс на `http://127.0.0.1:4040` где можно:
- Просматривать все HTTP запросы
- Повторять запросы
- Отлаживать проблемы

### Несколько туннелей одновременно

Создайте файл `ngrok.yml`:

```yaml
tunnels:
  frontend:
    proto: http
    addr: 3000
  backend:
    proto: http
    addr: 8000
```

Запустите:

```bash
ngrok start --all
```

## 🔧 Альтернативы ngrok

### localtunnel

```bash
npm install -g localtunnel
lt --port 3000
```

### serveo

```bash
ssh -R 80:localhost:3000 serveo.net
```

### cloudflared (Cloudflare Tunnel)

```bash
cloudflared tunnel --url http://localhost:3000
```

## ❓ Проблемы и решения

### "ERR_NGROK_108"

**Проблема:** Превышен лимит бесплатного плана

**Решение:** Подождите или используйте другой аккаунт

### "Tunnel not found"

**Проблема:** Неправильный authtoken

**Решение:** Проверьте токен на https://dashboard.ngrok.com/get-started/your-authtoken

### CORS ошибки

**Проблема:** Backend не разрешает запросы с ngrok домена

**Решение:** Добавьте ngrok URL в `CORS_ALLOWED_ORIGINS` (см. выше)

## 🔗 Полезные ссылки

- [Документация ngrok](https://ngrok.com/docs)
- [Dashboard ngrok](https://dashboard.ngrok.com/)
- [Telegram Login Widget](https://core.telegram.org/widgets/login)

---

**После настройки ngrok вы сможете полноценно тестировать Telegram авторизацию локально!**
