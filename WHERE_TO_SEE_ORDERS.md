# Где посмотреть созданные заказы

## ✅ РЕШЕНИЕ: Используйте главный дашборд

После создания заказа перейдите на **главный дашборд**:

```
URL: https://okoznaniy.ru/expert
или
URL: https://okoznaniy.ru/client-dashboard (автоматически перенаправит на /expert)
```

## Почему заказы не видны в `/orders-feed`?

Страница `/orders-feed` (Лента работ) предназначена **только для экспертов** и показывает:
- Доступные заказы **других клиентов**
- Заказы в статусе "new" без назначенного эксперта
- **НЕ показывает** ваши собственные заказы (чтобы вы не могли взять свой заказ в работу)

Это сделано специально для предотвращения конфликта интересов.

## Структура страниц

### Для клиентов:

| Страница | URL | Что показывает |
|----------|-----|----------------|
| **Главный дашборд** | `/expert` | ✅ **ВСЕ ВАШИ ЗАКАЗЫ** (созданные вами) |
| Создать заказ | `/create-order` | Форма создания нового заказа |
| Лента работ | `/orders-feed` | ❌ Не показывает ваши заказы (только чужие для экспертов) |

### Для экспертов:

| Страница | URL | Что показывает |
|----------|-----|----------------|
| **Главный дашборд** | `/expert` | ✅ Заказы, взятые в работу |
| Лента работ | `/orders-feed` | ✅ Доступные заказы для взятия в работу |

## Как проверить созданный заказ

### Шаг 1: Перейдите на главный дашборд
```
https://okoznaniy.ru/expert
```

### Шаг 2: Откройте вкладку "Заказы"
В дашборде есть несколько вкладок:
- Профиль
- **Заказы** ← Здесь ваши заказы!
- Специализации
- Статистика

### Шаг 3: Увидите список ваших заказов
Для клиентов отображаются:
- Все созданные заказы
- Статус каждого заказа
- Кнопка "Создать заказ"

## API Endpoints

### Для клиентов - свои заказы:
```
GET /api/users/client_orders/
```

### Для экспертов - доступные заказы:
```
GET /api/orders/orders/available/
```

### Для экспертов - заказы в работе:
```
GET /api/orders/orders/
```

## Логика работы системы

```
1. Клиент создаёт заказ
   ↓
2. Заказ появляется в дашборде клиента (/expert)
   ↓
3. Заказ появляется в ленте для экспертов (/orders-feed)
   ↓
4. Эксперты делают ставки
   ↓
5. Клиент видит ставки в своём дашборде
   ↓
6. Клиент принимает ставку
   ↓
7. Заказ назначается эксперту
   ↓
8. Эксперт выполняет работу
```

## Проверка через консоль браузера

Откройте консоль (F12) и выполните:

```javascript
// Получить свои заказы (для клиента)
fetch('https://okoznaniy.ru/api/users/client_orders/', {
  headers: {
    'Authorization': `Bearer ${localStorage.getItem('access_token')}`
  }
})
.then(r => r.json())
.then(data => console.log('Мои заказы:', data));
```

## Итог

✅ **Заказ создан успешно** - это видно в логах консоли  
✅ **Заказ сохранён в базе данных**  
✅ **Чтобы увидеть заказ, перейдите на `/expert` (главный дашборд)**  
❌ **НЕ ищите свои заказы в `/orders-feed` - там только чужие заказы для экспертов**

## Быстрая ссылка

👉 **[Перейти на главный дашборд](https://okoznaniy.ru/expert)**
