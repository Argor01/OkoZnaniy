# Быстрый старт: Telegram авторизация

## ✅ Что уже сделано

1. **Backend (Django)**
   - ✅ Добавлен endpoint `/api/users/telegram_auth/`
   - ✅ Реализована проверка подлинности данных Telegram
   - ✅ Автоматическое создание/обновление пользователей
   - ✅ Генерация JWT токенов
   - ✅ Токен бота добавлен в `.env`

2. **Frontend (React)**
   - ✅ Создан компонент `TelegramLoginButton`
   - ✅ Создан пример страницы `TelegramAuthExample`
   - ✅ Автоматическая отправка данных на backend
   - ✅ Сохранение токенов в localStorage

3. **Docker**
   - ✅ Добавлена переменная окружения `TELEGRAM_BOT_TOKEN`

## 🚀 Как использовать

### 1. Настройте бота в BotFather

Откройте [@BotFather](https://t.me/BotFather) и выполните:

```
/setdomain
@oko_expert_bot
localhost
```

Для продакшена замените `localhost` на ваш домен.

### 2. Запустите проект

**Вариант 1: Docker**
```bash
docker-compose up -d
```

**Вариант 2: Локально**
```bash
# Backend
python manage.py runserver

# Frontend (в другом терминале)
cd frontend-react
npm run dev
```

### 3. Добавьте кнопку на страницу логина

```tsx
import TelegramLoginButton from '../components/auth/TelegramLoginButton';

<TelegramLoginButton
  botName="oko_expert_bot"
  onAuth={(user) => {
    // Пользователь авторизован
    console.log('User:', user);
    navigate('/dashboard');
  }}
  onError={(error) => {
    // Ошибка авторизации
    console.error('Error:', error);
  }}
/>
```

### 4. Протестируйте

**Вариант 1: Через браузер**
1. Откройте страницу с кнопкой Telegram
2. Нажмите кнопку
3. Авторизуйтесь в Telegram

**Вариант 2: Через тестовый скрипт**
```bash
python test_telegram_auth.py
```

## 📝 API Endpoint

### POST `/api/users/telegram_auth/`

**Request:**
```json
{
  "id": 123456789,
  "first_name": "John",
  "last_name": "Doe",
  "username": "johndoe",
  "photo_url": "https://...",
  "auth_date": 1234567890,
  "hash": "abc123..."
}
```

**Response (200 OK):**
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "user": {
    "id": 1,
    "username": "johndoe",
    "telegram_id": 123456789,
    "role": "client",
    ...
  }
}
```

**Response (400 Bad Request):**
```json
{
  "error": "Неверные данные авторизации Telegram"
}
```

## 🔧 Настройка компонента

### Параметры TelegramLoginButton

```tsx
<TelegramLoginButton
  botName="oko_expert_bot"        // Обязательно: username бота
  buttonSize="large"               // Размер: large, medium, small
  cornerRadius={10}                // Радиус углов кнопки
  requestAccess={true}             // Запрашивать доступ к данным
  usePic={true}                    // Показывать аватар
  lang="ru"                        // Язык: ru, en, etc.
  onAuth={(user) => {}}            // Callback при успехе
  onError={(error) => {}}          // Callback при ошибке
/>
```

## 🔐 Безопасность

- ✅ Проверка HMAC-SHA256 hash
- ✅ Проверка времени авторизации (24 часа)
- ✅ JWT токены для API
- ✅ Токен бота только на сервере

## 🐛 Troubleshooting

### Кнопка не отображается
- Проверьте, что `botName` правильный
- Проверьте консоль браузера
- Убедитесь, что домен добавлен в BotFather

### Ошибка "Invalid hash"
- Проверьте токен бота в `.env`
- Убедитесь, что данные не изменены

### Ошибка "Bot domain invalid"
- Добавьте домен в BotFather через `/setdomain`
- Для локального тестирования используйте ngrok

## 📚 Дополнительно

Полная документация: [TELEGRAM_AUTH_SETUP.md](./TELEGRAM_AUTH_SETUP.md)

## 🎯 Следующие шаги

1. Интегрируйте кнопку в существующую страницу логина
2. Добавьте стилизацию под ваш дизайн
3. Настройте редиректы после авторизации
4. Добавьте функционал привязки Telegram к существующему аккаунту
5. Настройте продакшен домен в BotFather
