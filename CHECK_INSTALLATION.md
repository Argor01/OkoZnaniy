# ✅ Проверка установки функции "Баны за обмен контактами"

## Шаг 1: Проверка файлов

Убедитесь, что все файлы созданы:

```bash
# Backend файлы
dir apps\users\models.py
dir apps\users\views.py
dir apps\chat\models.py
dir apps\users\migrations\0002_add_contact_ban_fields.py
dir apps\chat\migrations\0002_add_contact_violation_log.py
dir apps\users\management\commands\create_test_banned_users.py

# Frontend файлы
dir frontend-react\src\pages\DirectorDashboard\DirectorDashboard.tsx
dir frontend-react\src\pages\DirectorDashboard\components\ContactBannedUsers\ContactBannedUsers.tsx
```

## Шаг 2: Применить миграции

```bash
python manage.py migrate
```

Вы должны увидеть:
```
Running migrations:
  Applying users.0002_add_contact_ban_fields... OK
  Applying chat.0002_add_contact_violation_log... OK
```

## Шаг 3: Создать тестовые данные

```bash
python manage.py create_test_banned_users
```

Вы должны увидеть:
```
✓ Создан/обновлен забаненный пользователь: banned_client1
✓ Создан/обновлен забаненный пользователь: banned_expert1
...
✓ Всего обработано: 5 пользователей
✓ Тестовые данные успешно созданы!
```

## Шаг 4: Запустить backend

```bash
python manage.py runserver
```

Сервер должен запуститься на http://127.0.0.1:8000/

## Шаг 5: Запустить frontend

Откройте НОВЫЙ терминал:

```bash
cd frontend-react
npm start
```

Frontend должен открыться на http://localhost:3000/

## Шаг 6: Проверить в браузере

1. Откройте http://localhost:3000/director-dashboard
2. Войдите как директор (если нужно создать: `python manage.py create_admin_accounts`)
3. В левом меню должен появиться пункт "Баны за обмен контактами" с иконкой 🛑

## Шаг 7: Проверить API напрямую

Откройте в браузере (после входа):
```
http://127.0.0.1:8000/api/users/contact_banned_users/
```

Должен вернуться JSON с забаненными пользователями.

## ❌ Если не видите новый пункт меню

### Вариант 1: Очистить кэш браузера
1. Нажмите Ctrl+Shift+R (жесткая перезагрузка)
2. Или откройте в режиме инкогнито

### Вариант 2: Перезапустить frontend
1. Остановите npm сервер (Ctrl+C)
2. Удалите кэш: `rmdir /s /q frontend-react\node_modules\.cache`
3. Запустите снова: `npm start`

### Вариант 3: Проверить консоль браузера
1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Проверьте, нет ли ошибок импорта

### Вариант 4: Проверить роль пользователя
Убедитесь, что вы вошли как пользователь с ролью 'admin' или 'arbitrator'

## 🔍 Отладка

### Проверить, что компонент импортируется
Откройте файл и проверьте импорт:
```bash
type frontend-react\src\pages\DirectorDashboard\DirectorDashboard.tsx | findstr "ContactBannedUsers"
```

Должно быть:
```
import ContactBannedUsers from './components/ContactBannedUsers/ContactBannedUsers';
...
component: <ContactBannedUsers />,
```

### Проверить структуру menuItems
В файле DirectorDashboard.tsx должен быть объект:
```typescript
{
  key: 'contact-bans',
  icon: <StopOutlined />,
  label: 'Баны за обмен контактами',
  component: <ContactBannedUsers />,
}
```

### Проверить, что файл компонента существует
```bash
dir frontend-react\src\pages\DirectorDashboard\components\ContactBannedUsers\ContactBannedUsers.tsx
```

## 📞 Если ничего не помогло

1. Сделайте скриншот консоли браузера (F12 → Console)
2. Проверьте логи backend сервера
3. Убедитесь, что миграции применены: `python manage.py showmigrations`
4. Проверьте, что вы вошли как admin/arbitrator

## ✅ Успешная установка

Если все работает, вы должны видеть:
- ✓ Новый пункт меню "Баны за обмен контактами"
- ✓ Таблицу с 5 тестовыми пользователями
- ✓ Фильтры и кнопки действий
- ✓ Возможность просмотра деталей и разбана
