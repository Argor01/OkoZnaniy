# Как протестировать создание претензий

## Проблема, которая была исправлена
Претензии из чата эксперта не создавались в базе данных - они отправлялись только как текстовые сообщения.

## Что было сделано
1. Добавлена функция `chatApi.createClaim()` в `frontend-react/src/api/chat.ts`
2. Обновлена функция `handleSubmitClaim` в `MessageModalNew.tsx`:
   - Создается объект Claim в БД перед отправкой сообщения
   - ID претензии включается в текст сообщения
   - Добавлен маппинг категорий на типы БД

## Как протестировать

### 1. Пересобрать фронтенд
```bash
docker-compose build frontend
docker-compose up -d frontend
```

### 2. Проверить текущие претензии в БД
```bash
python test_claims_creation.py
```

### 3. Создать претензию через UI
1. Войдите как эксперт
2. Откройте чат с технической поддержкой (иконка наушников)
3. Нажмите красную кнопку с восклицательным знаком (Подать претензию)
4. Выберите категорию претензии
5. Заполните текст претензии
6. Нажмите "Отправить"

### 4. Проверить, что претензия создалась
```bash
python test_claims_creation.py
```

Вы должны увидеть новую претензию с ID в списке.

### 5. Проверить в админ-панели
1. Войдите как администратор
2. Перейдите в раздел "Обращения"
3. Проверьте, что претензия отображается в списке

## Категории претензий и их маппинг

| Категория в UI | Тип в БД | Описание |
|----------------|----------|----------|
| Заказ не выполнен | refund | Возврат средств |
| Заказ выполнен некачественно/частично | quality | Качество работы |
| Заказ не оплачен | refund | Возврат средств |
| Необоснованный отзыв | complaint | Жалоба |
| Магазин готовых работ | complaint | Жалоба |
| Другое | other | Другое |

## Что проверить

✅ Претензия создается в БД с правильным типом
✅ ID претензии включается в сообщение: `🚨 ПРЕТЕНЗИЯ #123: Категория`
✅ Претензия отображается в админ-панели
✅ Если указан заказ, он привязывается к претензии
✅ Файлы прикрепляются к сообщению (если были добавлены)

## Дата
14 февраля 2026
