# 🏗️ Архитектура чата и уведомлений

## 📊 Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend (React)                      │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────────┐  ┌──────────────────┐                │
│  │  DashboardHeader │  │  MessagesModal   │                │
│  │  - Иконки        │  │  - Список чатов  │                │
│  │  - Счетчики      │  │  - Сообщения     │                │
│  └────────┬─────────┘  └────────┬─────────┘                │
│           │                     │                            │
│  ┌────────▼─────────────────────▼─────────┐                │
│  │     NotificationSystem                  │                │
│  │     - Уведомления                       │                │
│  │     - Настройки                         │                │
│  └────────┬────────────────────────────────┘                │
│           │                                                  │
│  ┌────────▼────────────────────────────────┐                │
│  │         Hooks (Auto-refresh)            │                │
│  │  ┌──────────┐      ┌──────────────┐    │                │
│  │  │ useChat  │      │useNotifications│   │                │
│  │  │ (15 sec) │      │   (30 sec)     │   │                │
│  │  └────┬─────┘      └──────┬─────────┘   │                │
│  └───────┼────────────────────┼─────────────┘                │
│          │                    │                              │
│  ┌───────▼────────────────────▼─────────────┐                │
│  │            API Clients                    │                │
│  │  ┌──────────┐      ┌──────────────┐     │                │
│  │  │ chatApi  │      │notificationsApi│    │                │
│  │  └────┬─────┘      └──────┬─────────┘    │                │
│  └───────┼────────────────────┼──────────────┘                │
└──────────┼────────────────────┼───────────────────────────────┘
           │                    │
           │   HTTP/REST API    │
           │                    │
┌──────────▼────────────────────▼───────────────────────────────┐
│                      Backend (Django)                          │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                    API Endpoints                         │  │
│  │  /api/chat/chats/                                       │  │
│  │  /api/chat/chats/{id}/messages/                         │  │
│  │  /api/chat/chats/{id}/send_message/                     │  │
│  │  /api/notifications/                                     │  │
│  └────────┬────────────────────────────────────────────────┘  │
│           │                                                    │
│  ┌────────▼────────────────────────────────────────────────┐  │
│  │                    ViewSets                              │  │
│  │  ┌──────────────┐      ┌──────────────────┐            │  │
│  │  │ ChatViewSet  │      │NotificationViewSet│            │  │
│  │  └──────┬───────┘      └────────┬──────────┘            │  │
│  └─────────┼──────────────────────┼─────────────────────────┘  │
│            │                      │                            │
│  ┌─────────▼──────────────────────▼─────────────────────────┐  │
│  │                    Serializers                            │  │
│  │  ┌──────────────┐      ┌──────────────────┐             │  │
│  │  │ChatSerializer│      │NotificationSerial.│             │  │
│  │  └──────┬───────┘      └────────┬──────────┘             │  │
│  └─────────┼──────────────────────┼──────────────────────────┘  │
│            │                      │                            │
│  ┌─────────▼──────────────────────▼─────────────────────────┐  │
│  │                      Models                               │  │
│  │  ┌──────┐  ┌─────────┐  ┌──────────────┐                │  │
│  │  │ Chat │  │ Message │  │ Notification │                │  │
│  │  └──┬───┘  └────┬────┘  └──────┬───────┘                │  │
│  └─────┼───────────┼───────────────┼────────────────────────┘  │
└────────┼───────────┼───────────────┼─────────────────────────┘
         │           │               │
         └───────────▼───────────────┘
                     │
         ┌───────────▼────────────┐
         │      PostgreSQL        │
         │   (или SQLite в dev)   │
         └────────────────────────┘
```

## 🔄 Поток данных

### Отправка сообщения

```
1. Пользователь вводит текст
   ↓
2. MessagesModal.handleSendMessage()
   ↓
3. chatApi.sendMessage(chatId, text)
   ↓
4. POST /api/chat/chats/{id}/send_message/
   ↓
5. ChatViewSet.send_message()
   ↓
6. Message.objects.create()
   ↓
7. NotificationService.notify_new_message()
   ↓
8. Notification.objects.create()
   ↓
9. Response → Frontend
   ↓
10. Обновление UI
```

### Получение сообщений

```
1. useChat hook (каждые 15 сек)
   ↓
2. chatApi.getChats()
   ↓
3. GET /api/chat/chats/
   ↓
4. ChatViewSet.list()
   ↓
5. Chat.objects.filter(participants=user)
   ↓
6. ChatSerializer
   ↓
7. Response → Frontend
   ↓
8. Обновление счетчиков
```

## 📁 Структура файлов

### Backend
```
OkoZnaniy/
├── apps/
│   ├── chat/
│   │   ├── models.py          # Chat, Message
│   │   ├── views.py           # ChatViewSet
│   │   ├── serializers.py     # ChatSerializer, MessageSerializer
│   │   ├── urls.py            # API routes
│   │   └── apps.py
│   └── notifications/
│       ├── models.py          # Notification
│       ├── views.py           # NotificationViewSet
│       ├── serializers.py
│       └── services.py        # NotificationService
├── config/
│   ├── settings.py            # INSTALLED_APPS += 'apps.chat'
│   └── urls.py                # path('api/chat/', ...)
└── setup-chat.bat             # Скрипт миграций
```

### Frontend
```
frontend-react/
├── src/
│   ├── api/
│   │   ├── chat.ts            # chatApi
│   │   └── notifications.ts   # notificationsApi
│   ├── hooks/
│   │   ├── useChat.ts         # Автообновление чатов
│   │   └── useNotifications.ts # Автообновление уведомлений
│   ├── components/
│   │   ├── MessagesModal.tsx  # Компонент чата
│   │   ├── notifications/
│   │   │   └── NotificationSystem.tsx
│   │   ├── common/
│   │   │   └── DashboardHeader.tsx
│   │   └── DashboardWithChatAndNotifications.tsx
│   └── config/
│       └── api.ts             # API_URL
└── INTEGRATION_EXAMPLE.md
```

## 🔐 Безопасность

### Аутентификация
```
Frontend                    Backend
   │                          │
   ├─ localStorage.getItem('access_token')
   │                          │
   ├─ Authorization: Bearer {token}
   │                          │
   └──────────────────────────▶ JWT Verification
                              │
                              ├─ User authenticated
                              │
                              └─ Access granted
```

### Проверка прав доступа
```python
# В ChatViewSet
def get_queryset(self):
    return Chat.objects.filter(
        participants=self.request.user
    )

def send_message(self, request, pk=None):
    chat = self.get_object()
    if request.user not in chat.participants.all():
        return Response(status=403)
```

### Защита от контактов
```python
# В Message.clean()
def clean(self):
    if re.search(r"(?:@|\+7|https?://|\d{9,})", self.text):
        raise ValidationError("Контактные данные запрещены")
```

## ⚡ Оптимизация

### Автообновление
- Чаты: каждые 15 секунд
- Уведомления: каждые 30 секунд
- Можно настроить в хуках

### Кэширование
```python
# В ViewSet
def get_queryset(self):
    return Chat.objects.filter(
        participants=self.request.user
    ).prefetch_related('participants', 'messages')
```

### Пагинация
```python
# В settings.py
REST_FRAMEWORK = {
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 20
}
```

## 🚀 Масштабирование

### WebSocket (будущее)
```
Frontend                    Backend
   │                          │
   ├─ WebSocket connection    │
   │                          │
   ├─────────────────────────▶ Django Channels
   │                          │
   │                          ├─ Redis (pub/sub)
   │                          │
   │◀─────────────────────────┤ Real-time messages
   │                          │
```

### Redis для кэша
```python
CACHES = {
    'default': {
        'BACKEND': 'django_redis.cache.RedisCache',
        'LOCATION': 'redis://127.0.0.1:6379/1',
    }
}
```

## 📊 Мониторинг

### Логирование
```python
import logging
logger = logging.getLogger(__name__)

def send_message(self, request, pk=None):
    logger.info(f"User {request.user.id} sent message to chat {pk}")
```

### Метрики
- Количество сообщений в день
- Среднее время ответа
- Количество активных чатов
- Количество непрочитанных

---

**Архитектура готова к использованию и масштабированию!** 🎉
