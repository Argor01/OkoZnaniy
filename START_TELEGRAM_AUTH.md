# 🚀 Запуск Telegram авторизации - 5 минут

## Шаг 1: Настройте бота (2 минуты)

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команды:

```
/setdomain
```

Выберите: `@oko_expert_bot`

Введите: `localhost` (для разработки)

**Готово!** ✅

## Шаг 2: Проверьте, что проект запущен (1 минута)

```bash
docker-compose ps
```

Должны работать:
- ✅ backend (порт 8000)
- ✅ frontend (порт 3000)
- ✅ postgres (порт 5432)
- ✅ redis (порт 6379)

Если не запущены:
```bash
docker-compose up -d
```

## Шаг 3: Добавьте кнопку на страницу (2 минуты)

Откройте вашу страницу логина и добавьте:

```tsx
import TelegramLoginButton from '../components/auth/TelegramLoginButton';

// В вашем компоненте
<TelegramLoginButton
  botName="oko_expert_bot"
  onAuth={(user) => {
    console.log('Авторизован:', user);
    navigate('/dashboard');
  }}
  onError={(error) => {
    alert('Ошибка: ' + error);
  }}
/>
```

## Шаг 4: Тест (30 секунд)

1. Откройте страницу в браузере
2. Нажмите кнопку "Login with Telegram"
3. Подтвердите в Telegram
4. Готово! ✅

## 🎉 Всё работает!

Теперь пользователи могут входить через Telegram в 1 клик.

---

## 📝 Дополнительно

### Пример готовой страницы

Откройте в браузере:
```
http://localhost:3000/telegram-auth-example
```

### Тестовый скрипт

```bash
python test_telegram_auth.py
```

### Проверка API

```bash
# Windows PowerShell
Invoke-WebRequest -Uri "http://localhost:8000/api/users/telegram_auth/" -Method OPTIONS

# Должен вернуть: StatusCode 200
```

---

## 🔧 Если что-то не работает

### Кнопка не появляется?
```tsx
// Проверьте, что botName правильный
botName="oko_expert_bot"  // ✅ Правильно
botName="oko_expert"      // ❌ Неправильно
```

### Ошибка "Bot domain invalid"?
```
Повторите Шаг 1 - настройте домен в BotFather
```

### Backend не отвечает?
```bash
# Проверьте логи
docker-compose logs backend

# Перезапустите
docker-compose restart backend
```

---

## 📚 Документация

- **Простая инструкция:** [TELEGRAM_ИНСТРУКЦИЯ.md](./TELEGRAM_ИНСТРУКЦИЯ.md)
- **Быстрый старт:** [TELEGRAM_AUTH_QUICKSTART.md](./TELEGRAM_AUTH_QUICKSTART.md)
- **Полная настройка:** [TELEGRAM_AUTH_SETUP.md](./TELEGRAM_AUTH_SETUP.md)
- **Резюме:** [TELEGRAM_AUTH_SUMMARY.md](./TELEGRAM_AUTH_SUMMARY.md)

---

## ✅ Готово!

**Токен бота:** `8584999235:AAGKcP0nhnn_B6G8iTa2Ti8U9oxUFByWfpo`  
**Username бота:** `@oko_expert_bot`  
**API Endpoint:** `POST /api/users/telegram_auth/`

Авторизация через Telegram работает! 🎉
