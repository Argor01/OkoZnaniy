# 🚀 Автоматический запуск с чатом

## ✨ У вас уже есть готовые скрипты!

### 🐳 Для Docker (рекомендуется)

**Один скрипт делает всё:**
```bash
cd OkoZnaniy
./update-and-restart.sh
```

**Что он делает:**
1. ✅ Получает изменения из git
2. ✅ Останавливает контейнеры
3. ✅ Пересобирает контейнеры
4. ✅ Запускает контейнеры
5. ✅ **Применяет миграции чата** (новое!)
6. ✅ Готово!

### 💻 Для локальной разработки

**Два простых скрипта:**

1. **Настройка чата** (один раз):
```bash
cd OkoZnaniy
setup-chat.bat
```

2. **Запуск проекта**:
```bash
start-dev.bat
```

## 📋 Что было обновлено

### `update-and-restart.sh`
- ✅ Добавлено автоматическое применение миграций чата
- ✅ Добавлена проверка успешности миграций
- ✅ Добавлена ссылка на API чата

### `setup-chat.bat`
- ✅ Добавлена автоматическая проверка Docker
- ✅ Работает и с Docker, и локально
- ✅ Умный выбор способа запуска

## 🎯 Использование

### Первый запуск

**Docker:**
```bash
cd OkoZnaniy
./update-and-restart.sh
```

**Локально:**
```bash
cd OkoZnaniy
setup-chat.bat
start-dev.bat
```

### Последующие запуски

**Docker:**
```bash
./update-and-restart.sh
```

**Локально:**
```bash
start-dev.bat
```

## ✅ Проверка

После запуска откройте:
- 🌐 Frontend: http://localhost:3000
- 🔧 Backend: http://localhost:8000
- 💬 API чата: http://localhost:8000/api/chat/chats/
- 🔔 API уведомлений: http://localhost:8000/api/notifications/

## 🎉 Готово!

Теперь при каждом запуске `update-and-restart.sh`:
- ✅ Код обновляется
- ✅ Контейнеры пересобираются
- ✅ Миграции чата применяются автоматически
- ✅ Всё работает!

## 📝 Дополнительные скрипты

### Остановка
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs -f
```

### Статус контейнеров
```bash
docker-compose ps
```

## 🐛 Если что-то пошло не так

### Миграции не применились
```bash
docker-compose exec web python manage.py migrate
```

### Контейнеры не запускаются
```bash
docker-compose down
docker-compose up --build
```

### Нужно пересоздать всё
```bash
docker-compose down -v
docker-compose up --build
```

## 💡 Совет

Добавьте алиас в `.bashrc` или `.zshrc`:
```bash
alias oz-restart='cd ~/path/to/OkoZnaniy && ./update-and-restart.sh'
```

Теперь можно запускать из любой папки:
```bash
oz-restart
```

---

**Всё настроено и готово к использованию!** 🎉
