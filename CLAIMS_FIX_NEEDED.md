# Исправление: Претензии из чата эксперта теперь создаются в БД

## Проблема (РЕШЕНА)
Когда эксперт подавал претензию через чат, она отправлялась только как текстовое сообщение в чат с поддержкой, но не создавался объект Claim в базе данных.

## Решение
Обновлена функция `handleSubmitClaim` в `MessageModalNew.tsx`:
1. Создается объект Claim в БД через `chatApi.createClaim()`
2. Затем отправляется сообщение в чат со ссылкой на созданную претензию (ID претензии)
3. Добавлен маппинг категорий претензий на типы БД

## Маппинг категорий претензий
Категории в UI → Типы в БД:
- 'Заказ не выполнен' → 'refund'
- 'Заказ выполнен некачественно/частично' → 'quality'
- 'Заказ не оплачен' → 'refund'
- 'Необоснованный отзыв' → 'complaint'
- 'Магазин готовых работ' → 'complaint'
- 'Другое' → 'other'

## Изменения в коде

### 1. API функция (frontend-react/src/api/chat.ts)
✅ Добавлена функция `chatApi.createClaim()`

### 2. Компонент MessageModalNew.tsx
✅ Обновлена функция `handleSubmitClaim`:
- Добавлен маппинг категорий на типы БД
- Вызов `chatApi.createClaim()` перед отправкой сообщения
- ID претензии включается в текст сообщения: `🚨 ПРЕТЕНЗИЯ #${claim.id}: ${категория}`

## Статус
✅ ИСПРАВЛЕНО - Претензии теперь создаются в БД и отображаются в админ-панели

## Дата
14 февраля 2026
