# Быстрое исправление: "объект не существует"

## Проблема
```
Предмет: Недопустимый первичный ключ "1" - объект не существует.
Тип работы: Недопустимый первичный ключ "1" - объект не существует.
```

## Причина
В базе данных нет предметов и типов работ.

## Решение (автоматическое)

Просто запустите скрипт обновления:
```bash
cd ~/OkoZnaniy
./update-and-restart.sh
```

Скрипт автоматически:
1. Скачает последние изменения
2. Пересоберёт контейнеры
3. Применит миграции
4. **Заполнит справочники предметов и типов работ**

## Решение (ручное)

Если автоматическое не сработало, запустите вручную:

```bash
cd ~/OkoZnaniy
docker-compose exec backend python populate_subjects_and_work_types.py
```

Вы увидите:
```
📚 Заполнение справочников предметов и типов работ
📖 Создание предметов...
✅ Создан предмет: Математика (ID: 1)
✅ Создан предмет: Другое (ID: 2)
...
📝 Создание типов работ...
✅ Создан тип работы: Курсовая работа (ID: 1, цена: 3000₽)
✅ Создан тип работы: Другое (ID: 2, цена: 1000₽)
...
✅ Справочники успешно заполнены!
```

## Проверка

После заполнения справочников попробуйте создать заказ снова.

### Что будет создано:

**Предметы (15 шт):**
- Математика
- Другое
- Физика
- Химия
- Биология
- История
- Литература
- Русский язык
- Английский язык
- Информатика
- Экономика
- Право
- Философия
- Психология
- Социология

**Типы работ (10 шт):**
- Курсовая работа (3000₽, 14 дней)
- Другое (1000₽, 7 дней)
- Дипломная работа (15000₽, 30 дней)
- Реферат (500₽, 3 дня)
- Эссе (800₽, 3 дня)
- Контрольная работа (1000₽, 5 дней)
- Лабораторная работа (1500₽, 7 дней)
- Решение задач (500₽, 2 дня)
- Презентация (1000₽, 3 дня)
- Отчёт по практике (2000₽, 7 дней)

## Если не помогло

Проверьте что скрипт выполнился без ошибок:
```bash
docker-compose logs backend | grep "populate_subjects"
```

Или запустите через Django shell:
```bash
docker-compose exec backend python manage.py shell
```

```python
from apps.catalog.models import Subject, WorkType

# Проверить количество
print(f"Subjects: {Subject.objects.count()}")
print(f"WorkTypes: {WorkType.objects.count()}")

# Посмотреть первые записи
print("\nПервые предметы:")
for s in Subject.objects.all()[:5]:
    print(f"  {s.id}: {s.name}")

print("\nПервые типы работ:")
for w in WorkType.objects.all()[:5]:
    print(f"  {w.id}: {w.name} - {w.base_price}₽")
```
