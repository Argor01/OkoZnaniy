# 🔍 Как проверить что чат эксперта и админка связаны с бэкендом

## На хостинге выполните:

```bash
cd ~/OkoZnaniy
docker-compose exec backend python test_expert_chat_and_admin.py
```

Скрипт проверит:
- ✅ Чаты эксперта (наличие, сообщения)
- ✅ API эндпоинты чата
- ✅ Пользователей в админке
- ✅ Заказы в админке
- ✅ API эндпоинты админки
- ✅ Модели админки (SupportRequest, Claim, AdminChatRoom)

---

## Проверка на фронтенде

### 1. Проверка чата эксперта

```bash
# Войдите на сайт как эксперт
Логин: expert1@test.com
Пароль: test123
```

**Что проверить:**
1. Откройте иконку чата (💬)
2. Должен загрузиться список чатов (не моки!)
3. Откройте любой чат
4. Отправьте сообщение
5. Прикрепите файл
6. Проверьте что сообщения сохраняются

**Как понять что это реальные данные:**
- Откройте DevTools (F12) → Network
- При открытии чата должен быть запрос: `GET /api/chat/chats/`
- При отправке сообщения: `POST /api/chat/chats/{id}/send_message/`
- Если видите эти запросы → всё работает с бэкендом ✅

---

### 2. Проверка админки

```bash
# Войдите на сайт как админ
Логин: admin@test.com
Пароль: test123
```

**Что проверить:**
1. Откройте раздел "Управление пользователями"
2. Должен загрузиться список пользователей (не моки!)
3. Откройте раздел "Все заказы"
4. Должен загрузиться список заказов
5. Откройте раздел "Поддержка"
6. Откройте раздел "Обращения"

**Как понять что это реальные данные:**
- Откройте DevTools (F12) → Network
- При открытии разделов должны быть запросы:
  - `GET /api/admin-panel/users/`
  - `GET /api/admin-panel/orders/`
  - `GET /api/admin-panel/support-requests/`
  - `GET /api/admin-panel/claims/`
- Если видите эти запросы → всё работает с бэкендом ✅

---

## Быстрая проверка через curl

### Проверка API чата:
```bash
# Получить список чатов (нужен токен авторизации)
curl -H "Authorization: Bearer YOUR_TOKEN" http://45.12.239.226/api/chat/chats/
```

### Проверка API админки:
```bash
# Получить статистику админки (нужен токен админа)
curl -H "Authorization: Bearer ADMIN_TOKEN" http://45.12.239.226/api/admin-panel/stats/
```

---

## Что делать если данные не загружаются

### 1. Проверьте логи бэкенда:
```bash
docker-compose logs -f backend
```

### 2. Проверьте что миграции применены:
```bash
docker-compose exec backend python manage.py showmigrations
```

### 3. Создайте тестовые данные:
```bash
docker-compose exec backend python manage.py create_test_users
docker-compose exec backend python setup_test_chats.py
```

---

## Признаки что всё работает

### ✅ Чат эксперта связан с бэкендом:
- Нет слова "mock" в коде `MessageModalNew.tsx`
- Используется `chatApi` из `frontend-react/src/api/chat.ts`
- В Network видны запросы к `/api/chat/`
- Сообщения сохраняются после перезагрузки страницы

### ✅ Админка связана с бэкендом:
- Используются хуки из `useAdminPanelData.ts`
- В Network видны запросы к `/api/admin-panel/`
- Данные обновляются при действиях (блокировка пользователя и т.д.)
- Статистика показывает реальные цифры

---

## Если всё равно не уверены

Выполните на хосте:
```bash
cd ~/OkoZnaniy
docker-compose exec backend python test_expert_chat_and_admin.py
```

Скрипт покажет детальный отчет о состоянии чата и админки.
